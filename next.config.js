/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'randomuser.me',
      'source.unsplash.com',
      'cdn.iconscout.com'
    ],
  },
  reactStrictMode: true,
};

module.exports = nextConfig; 